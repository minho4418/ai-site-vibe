-- =========================================================================
-- Migration 003 — 기사 키워드(커버 표지용)
-- 썸네일이 없는 기사의 카드 커버에 큰 타이포로 얹을 핵심 키워드 2~3개를
-- Groq 요약 단계에서 함께 추출해 저장한다(ai_summary 와 같은 호출).
-- 채움은 ingest 크론의 enrichSummaries 가 담당(점진적 백필).
-- 재실행 안전(IF NOT EXISTS).
-- =========================================================================

alter table public.articles add column if not exists keywords text[];
